# Surrogate Factory v2.2 - environment to run the pipelines
# Versions taken from the working .venv (Python 3.11.15). Use Python 3.11.
#
# Install:
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)   |   source .venv/bin/activate (Mac/Linux)
#   pip install -r requirements.txt
#   pip install -e .                (from the Pipelines repo root: installs surrogate_factory from src/)
#
# Note: the executive summary PDF needs a LaTeX distribution (e.g. MiKTeX on
# Windows, TeX Live otherwise). That is a system install, not a pip package.

# --- scientific core ---
numpy==2.4.6
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.9.0
statsmodels==0.14.6
sympy==1.14.0
joblib==1.5.3
tqdm==4.68.3

# --- models beyond sklearn ---
xgboost==3.2.0
torch==2.12.1

# --- plotting ---
matplotlib==3.11.0
seaborn==0.13.2

# --- framework dependencies (surrogate_factory) ---
PyYAML==6.0.3
jsonschema==4.26.0
fsspec==2026.6.0
toolz==1.1.0
cytoolz==1.1.0
openpyxl==3.1.5
orjson==3.11.9
packaging==25.0

# --- tracking and reporting ---
mlflow==3.14.0
papermill==2.7.0
nbclient==0.11.0
nbconvert==7.17.1
nbformat==5.10.4
jinja2==3.1.6

# --- notebooks / pipeline editor ---
jupyterlab==4.6.0
elyra==4.1.1
ipython==9.10.1
ipywidgets==8.1.8
