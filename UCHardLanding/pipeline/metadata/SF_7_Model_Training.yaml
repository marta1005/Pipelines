Model_Training:
  # Hyperparameters and max_iter are defined in SF_6_Model_Selection.yaml.
  # This step trains both models (MLP and GradientBoosting) and saves them.
  note: "Two models trained — MLP (multioutput) and GradientBoosting (one regressor per output)."
